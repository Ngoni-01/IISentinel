# IISentinel v3.0 - Python dependencies
# Install: pip install -r requirements.txt
 
# ── Core web framework (async) ──────────────────────────────────────────────
quart>=0.19.0
quart-cors>=0.7.0
hypercorn>=0.16.0          # Production ASGI server
 
# ── Machine learning ─────────────────────────────────────────────────────────
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0
 
# ── PDF export ───────────────────────────────────────────────────────────────
reportlab>=4.0.0
 
# ── HTTP client (weather API, notifications) ─────────────────────────────────
requests>=2.31.0
 
# ── Optional: Supabase cloud database (SQLite used by default) ───────────────
# supabase>=2.0.0
 
# ── Optional: SMS via Twilio (Africa's Talking used by default) ──────────────
# twilio>=8.0.0